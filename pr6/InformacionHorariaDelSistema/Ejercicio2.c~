// Para time
#include <time.h>
// Para printf
#include <stdio.h>

int main( int argc, char **argv )
{
	// time_t time(time_t *t)
	time_t fecha = time(NULL);

	printf("-> Fecha: %d\n", fecha);

	return 0;
}
